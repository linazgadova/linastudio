node_modules
dist
.DS_Store
*.local
.vite

# Папка для выкладки, собирается командой npm run pack
site

# Ключи не коммитим
.env
.env.*
!.env.example

# Настройки с ключом модели живут только на сервере
public/api/config.php

# Копии удалённых файлов — наружу не отдаём
.backup

# Остатки от Cloudflare, от которого ушли
.wrangler
.netlify

# Исходные материалы: демо и презентация «Рабочего места».
# Система закрытая, на сайте она намеренно без ссылки наружу —
# выкладывать её демо и питч в открытый репозиторий нельзя.
# В сборке эта папка не участвует, проверено поиском по импортам.
assets-source

# Служебные папки инструментов
.impeccable
.playwright-mcp
scripts/.survey
scripts/.clips-tmp

# Черновые заметки по продукту — рабочий документ, наружу не идёт
PRODUCT.md

# Правила работы над проектом — пока держим при себе
CLAUDE.md
