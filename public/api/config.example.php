<?php
/**
 * НАСТРОЙКИ БЛОКА С ВОПРОСАМИ — ОБРАЗЕЦ
 *
 * Рабочие настройки лежат рядом в config.php — копии этого файла
 * со своими значениями. Сам config.php в исходниках отсутствует
 * и в сборку не попадает: ключ не должен оказаться ни в репозитории,
 * ни в папке, которую заливают на хостинг целиком.
 *
 * Заливка сайта заново его не затирает: файлы копируются поверх,
 * а config.php среди них нет.
 *
 * Без этого файла блок с вопросами отвечает поиском по данным
 * самого сайта, без модели. Это рабочий режим, он честно подписан.
 */

return [
    /*
     * 'yandex' — YandexGPT. Единственный, что работает с российского
     *            хостинга без плясок с сертификатами.
     * 'openai' — любой с привычным форматом: Groq, GigaChat, OpenAI.
     *            Тогда нужен ещё 'base'.
     */
    'provider' => 'yandex',

    // YandexGPT: ключ сервисного аккаунта и идентификатор каталога.
    // Оба берутся в консоли Yandex Cloud, каталог — строка вида b1g...
    'key' => 'сюда-ключ',
    'folder' => 'сюда-идентификатор-каталога',

    // yandexgpt-lite дешевле и быстрее, yandexgpt заметно умнее.
    // Для ответов на вопросы о человеке хватает lite
    'model' => 'yandexgpt-lite',

    // Только для provider = 'openai'. Адрес до /chat/completions,
    // например https://api.groq.com/openai/v1
    'base' => '',

    /*
     * Потолок вопросов к модели на весь сайт за сутки.
     *
     * Верхняя граница счёта, а не ограничение для посетителей: один
     * вопрос стоит около полурубля, сто вопросов в день это полсотни
     * рублей. Упереться не страшно — блок перейдёт на поиск по данным
     * сайта, тот же режим, что работал до появления ключа.
     *
     * Ноль снимает потолок совсем. Так делать не стоит.
     */
    'dailyCap' => 100,
];
